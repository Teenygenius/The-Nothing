# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
# If left blank or connection fails, the server will automatically start an in-memory database instance
MONGODB_URI=mongodb://127.0.0.1:27017/nothing

# Security
JWT_SECRET=nothing_super_secret_enterprise_void_jwt_key_2026

# Client Configuration (used by Vite)
VITE_API_URL=http://localhost:5000/api
